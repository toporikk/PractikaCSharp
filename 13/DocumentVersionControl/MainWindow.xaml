<Window x:Class="DocumentVersionControl.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:DocumentVersionControl.ViewModels"
        mc:Ignorable="d"
        Title="Система контроля версий документов" Height="500" Width="800"
        Focusable="True">

    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Menu -->
        <Menu Grid.Row="0">
            <MenuItem Header="Файл">
                <MenuItem Header="Выход" InputGestureText="Alt+F4" Click="ExitClick"/>
            </MenuItem>
            <MenuItem Header="Документы">
                <MenuItem Header="Добавить версию" InputGestureText="Ctrl+N" Command="{Binding AddVersionCommand}"/>
                <MenuItem Header="Просмотреть историю" InputGestureText="Ctrl+H" Command="{Binding ViewHistoryCommand}"/>
                <MenuItem Header="Удалить версию" InputGestureText="Ctrl+D" Command="{Binding DeleteVersionCommand}"/>
            </MenuItem>
            <MenuItem Header="Настройки">
                <MenuItem Header="О программе" Click="AboutClick"/>
            </MenuItem>
        </Menu>

        <!-- ToolBar -->
        <ToolBar Grid.Row="1">
            <Button Command="{Binding AddVersionCommand}" ToolTip="Добавить версию (Ctrl+N)">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="➕ "/>
                    <TextBlock Text="Добавить версию"/>
                    <TextBlock Text=" (Ctrl+N)" Foreground="Gray" FontSize="10"/>
                </StackPanel>
            </Button>
            <Button Command="{Binding ViewHistoryCommand}" ToolTip="Просмотреть историю (Ctrl+H)">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="📋 "/>
                    <TextBlock Text="История"/>
                    <TextBlock Text=" (Ctrl+H)" Foreground="Gray" FontSize="10"/>
                </StackPanel>
            </Button>
            <Button Command="{Binding DeleteVersionCommand}" ToolTip="Удалить версию (Ctrl+D)">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="❌ "/>
                    <TextBlock Text="Удалить версию"/>
                    <TextBlock Text=" (Ctrl+D)" Foreground="Gray" FontSize="10"/>
                </StackPanel>
            </Button>
        </ToolBar>

        <!-- DataGrid -->
        <DataGrid Grid.Row="2" 
                  ItemsSource="{Binding Versions}" 
                  SelectedItem="{Binding SelectedVersion}"
                  AutoGenerateColumns="False"
                  Margin="10"
                  IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Версия" Binding="{Binding VersionNumber}" Width="100"/>
                <DataGridTextColumn Header="Дата создания" Binding="{Binding CreatedDate}" Width="150"/>
                <DataGridTextColumn Header="Описание" Binding="{Binding Description}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Status Bar -->
        <StatusBar Grid.Row="3">
            <StatusBarItem>
                <TextBlock Text="Готов к работе"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>